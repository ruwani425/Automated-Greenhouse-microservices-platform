spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: C:/Documents/GDSE/Microservices/agms-microservices-platform/config-repo

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/